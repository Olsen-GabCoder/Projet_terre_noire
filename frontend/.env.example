# URL de l'API backend
# En local : même host que le frontend pour les cookies JWT (127.0.0.1 ou localhost)
# En production (ex. Render) : définir dans l'environnement du build, ex. :
#   VITE_API_URL=https://maison-edition-api.onrender.com/api
VITE_API_URL=http://127.0.0.1:8000/api
