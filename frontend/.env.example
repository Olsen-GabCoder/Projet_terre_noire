# URL de l'API backend
# Important : utilisez le MÊME host que pour accéder au frontend (127.0.0.1 ou localhost)
# pour que les cookies JWT fonctionnent (session persistante au rechargement)
VITE_API_URL=http://127.0.0.1:8000/api
